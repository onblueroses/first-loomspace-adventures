 "This might seem to imply that $U$ is a subset of $\mathbb{R}^3$, but it's not! $U$ is a subset of $M$, and the points of $M$ are not points in $\mathbb{R}^3$ - they are points in the manifold. We can represent them by points in $\mathbb{R}^3$, but that representation is not the point itself. The manifold is an abstract space. The embedding of $M$ into $\mathbb{R}^3$ is a mapping from the manifold into $\mathbb{R}^3$, not the manifold itself."

I've thought about this a lot, and I can't make sense of it. If $M$ isn't a subset of $\mathbb{R}^3$, then how can it be embedded in $\mathbb{R}^3$? If I can embed $M$ in $\mathbb{R}^3$, doesn't that mean that there is a point in $\mathbb{R}^3$ that corresponds to every point in $M$? And by 'corresponds' I mean there's a bijection from $M$ to a subset of $\