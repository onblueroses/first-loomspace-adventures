 "The minimum of the function is $0$, which is achieved when $x=0$."

The function is:

$$f(x) = \frac{1}{x^2 + 1}$$

The first derivative is:

$$f'(x) = -\frac{2x}{(x^2 + 1)^2}$$

The second derivative is:

$$f''(x) = \frac{2(3x^2 - 1)}{(x^2 + 1)^3}$$

I see that since the denominator $(x^2 + 1)^3$ is always positive, the sign of the second derivative depends only on the sign of the numerator $2(3x^2 - 1)$.

I see that the function is concave up when $2(3x^2 - 1) > 0$, which simplifies to $3x^2 > 1$.

I see that the function is concave down when $2(3x^2 - 1) < 0$, which simplifies to $3x^2 < 1$.

But I don't see how the minimum of the function is $0$, which is achieved when $x=